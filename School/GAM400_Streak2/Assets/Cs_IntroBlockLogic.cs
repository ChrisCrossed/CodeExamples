using UnityEngine;
using System.Collections;

public class Cs_IntroBlockLogic : MonoBehaviour
{
    enum Enum_BlockColumn
    {
        RightSide,
        Center,
        LeftSide
    }

    [SerializeField] Enum_BlockColumn e_BlockColumn;

	// Use this for initialization
	void Start ()
    {
	    // if()
	}
	
	// Update is called once per frame
	void Update ()
    {
	
	}
}
